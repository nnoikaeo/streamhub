# Nuxt dev/build outputs
.output
.data/*
!.data/*.json
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# Firebase
.firebase

# Scratch prompts / working notes — throwaway, never committed
prompt-*.md

# QA fixture state (scripts/qa-broken-refs.mjs) — local only, holds the originals to restore
scripts/.qa-broken-refs-state.json
